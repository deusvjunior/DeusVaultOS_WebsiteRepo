# DeusVaultOS Backend Environment Variables

# SERVER CONFIGURATION
NODE_ENV=production
PORT=3001

# EMAIL CONFIGURATION (Required for production)
# Use your preferred email service (Gmail, SendGrid, Mailgun, etc.)

# SMTP Settings (Gmail example)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=your-email@gmail.com
SMTP_PASS=your-app-password

# Or use SendGrid (recommended for production)
# SMTP_HOST=smtp.sendgrid.net
# SMTP_PORT=587
# SMTP_SECURE=false
# SMTP_USER=apikey
# SMTP_PASS=your-sendgrid-api-key

# EMAIL ADDRESSES
FROM_EMAIL=hello@deusvault.com
TO_EMAIL=hello@deusvault.com

# SECURITY (Optional - defaults provided)
# CORS_ORIGINS=https://deusvault.com,https://deusvaultos.netlify.app

# RENDER DEPLOYMENT NOTES:
# 1. Set these environment variables in Render dashboard
# 2. For Gmail: Use App Password, not regular password
# 3. For SendGrid: Get API key from SendGrid dashboard
# 4. Test locally first with: npm run dev
